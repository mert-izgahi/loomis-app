export enum Role {
  Admin = "Admin",
  User = "User"
}

export enum ReportStatus {
  Pending = "Pending",
  Draft = "Draft",
  Published = "Published"
}

export enum ReportType {
  Internal = "Internal",
  External = "External"
}